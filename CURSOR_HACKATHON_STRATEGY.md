# Cursor Seoul Hackathon: 전략 가이드

> **Cursor Seoul Hackathon: AI Builder's Playground**
> 2026년 2월 7일(토) 10:00~18:00 | 서울 강남구 역삼로 557 제니스타워 4층
>
> 실제 코딩 시간: **5시간** (11:00~16:00)
> 사전 코딩 불가 — 기획·리서치만 사전 준비 가능

---

## 목차

1. [해커톤 핵심 정보](#1-해커톤-핵심-정보)
2. [Cursor 고급 기능 총정리](#2-cursor-고급-기능-총정리)
3. [wigtn 플러그인 경험의 Cursor 전환](#3-wigtn-플러그인-경험의-cursor-전환)
4. [사전 준비 (코드 없이)](#4-사전-준비-코드-없이)
5. [당일 5시간 실전 전략](#5-당일-5시간-실전-전략)
6. [발표 전략 (3~4분)](#6-발표-전략-34분)
7. [핵심 팁](#7-핵심-팁)
8. [참고 자료](#8-참고-자료)

---

## 1. 해커톤 핵심 정보

### 일정

```
09:30 - 10:00 │ 체크인 & 웰컴
10:00 - 10:30 │ 오프닝 & Cursor 활용 팁 공유
10:30 - 11:00 │ 팀빌딩 & 아이디어 브리핑
11:00 - 11:30 │ 해킹 시작 ← 코딩 시작점
11:30 - 16:00 │ 집중 개발 세션
       16:00  │ ⛔ 프로젝트 제출 마감
16:00 - 17:40 │ 팀별 발표 & 심사 (팀당 3~4분)
17:40 - 18:00 │ 시상, 단체 사진 & 네트워킹
```

### 심사 기준

| 기준 | 설명 | 전략적 비중 |
|---|---|---|
| **실용성** | 실제로 사람들이 쓸 만한가? | ★★★★★ |
| **창의성** | 문제 정의와 접근 방식이 새로운가? | ★★★★☆ |
| **완성도** | 제한된 시간 내 구현 수준 및 데모 안정성 | ★★★★☆ |
| **임팩트** | 사용자/시장/커뮤니티에 줄 수 있는 가치 | ★★★★☆ |

### 제약 조건

```
✅ 사전 준비 가능: 기획, 리서치, 아키텍처 설계, 기술 선정
✅ 사전 준비 가능: AGENTS.md, .cursor/rules/, MCP 설정, 환경 설정
✅ 기존 프로젝트 기반 가능: 단, 당일 추가 개발 부분이 명확해야 함
❌ 사전 코딩 불가: 코드는 당일 현장에서 작성 시작
```

### 팀 구성

| 역할 | 코드명 | 인원 | 강점 |
|---|---|---|---|
| AI 개발자 | **AI-1** | 1명 | AI/ML 파이프라인, 모델 API |
| AI 개발자 | **AI-2** | 1명 | 프롬프트 엔지니어링, QA, 발표 |
| 프론트엔드 | **FE-1** | 1명 | UI/UX, React/Next.js |
| 프론트엔드 (풀스택) | **FE-2** | 1명 | API 연동, 백엔드 로직 |

---

## 2. Cursor 고급 기능 총정리

### 2.1 Parallel Agents + Auto-Judging

하나의 프롬프트로 **최대 8개 에이전트를 동시 실행**하고, 완료 후 자동으로 최적 결과를 선택해주는 기능.

- 각 에이전트는 **git worktree**로 격리된 코드 사본에서 독립 작업
- 모든 에이전트 완료 후 **자동으로 최적 결과 선택** + 선택 이유 코멘트 제공
- 설정: 좌하단 `local` → `worktree` 로 변경해야 병렬 옵션(`1x`) 활성화

**사용 흐름:**

```
1. Plan Mode로 작업 계획 수립
2. 병렬 에이전트 실행 (최대 8개)
3. 각 에이전트가 git worktree에서 독립 작업
4. 전부 완료 → Auto-Judging이 최적 결과 선택 + 이유 설명
5. 선택된 결과 채택 또는 수동 머지
```

**5시간 해커톤에서 활용 예시:**

```
핵심 페이지를 만들 때 (FE-1):
→ Agent 1: 메인 랜딩 페이지
→ Agent 2: 대시보드 레이아웃
→ Agent 3: 결과 표시 페이지
→ 3개가 동시에 완성 → 각각 best pick 채택
→ 순차적으로 하면 3시간 → 병렬로 하면 1시간
```

**Best Practices:**
- 각 에이전트의 작업 범위가 겹치지 않도록 명확히 분리
- git worktree 사용 (충돌 방지)
- Plan Mode에서 먼저 계획 → 그다음 병렬 실행
- 5시간밖에 없으므로 **모든 독립 작업은 병렬로**

---

### 2.2 Agent Modes (3가지)

| 모드 | 단축키 | 역할 | 해커톤 용도 |
|---|---|---|---|
| **Plan Mode** | `Shift+Tab` | 계획만 세움, 코드 안 씀 | 11:00 첫 10분: 아키텍처 → 코드 구조 계획 |
| **Agent Mode** | 기본값 | 파일 읽기/쓰기/명령 실행 | 11:10~15:30: 실제 구현 |
| **Ask Mode** | - | 질문에만 답변 | 디버깅, 통합 이슈 해결 |

**핵심 원칙:** Plan Mode 먼저 → Agent Mode 나중

> "The #1 cause of bad AI code is skipping the planning phase."
> AI에게 "바로 코딩해"보다 "계획 세워"가 품질 훨씬 높음

---

### 2.3 Rules 시스템 (4가지 레벨)

| 타입 | 위치 | 범위 | 사전 준비 가능 |
|---|---|---|---|
| **Project Rules** | `.cursor/rules/*.mdc` | 프로젝트, glob 스코핑 | ✅ 미리 작성 |
| **User Rules** | Cursor Settings | 개인 전역 | ✅ 미리 설정 |
| **Team Rules** | Cursor Dashboard | 팀 자동 적용 | ✅ 미리 설정 |
| **AGENTS.md** | 프로젝트 루트 | 모든 AI 도구 호환 | ✅ 미리 작성 |

> **중요:** Rules와 AGENTS.md는 코드가 아니라 **설정 파일**이므로 사전 준비 가능.
> 이것이 해커톤에서 가장 큰 차이를 만드는 사전 준비 포인트.

#### AGENTS.md

AI를 위한 README. 새 채팅마다 메모리가 리셋되지만, AGENTS.md는 항상 참조됨.

```markdown
# Project: [프로젝트명]

## Architecture
- Frontend: Next.js 16 App Router + TailwindCSS
- Backend: Supabase (Auth, DB, Realtime)
- AI: OpenAI API / Anthropic API
- Deploy: Vercel

## Conventions
- TypeScript strict mode
- 컴포넌트: src/components/[Feature]/index.tsx
- API: src/app/api/[route]/route.ts
- 한국어 주석 OK, 변수명은 영어

## Folder Structure
src/
├── app/          # Next.js App Router
├── components/   # React 컴포넌트
├── lib/          # 유틸리티, AI 클라이언트
├── hooks/        # 커스텀 훅
└── types/        # TypeScript 타입

## Current Tasks
- [AI-1] AI 파이프라인 구축
- [AI-2] 프롬프트 엔지니어링
- [FE-1] UI/UX 핵심 페이지
- [FE-2] API + 백엔드 로직
```

**특징:**
- Codex, Gemini, Cursor, VS Code 등 **다수 도구에서 표준으로 수렴 중**
- 중첩 AGENTS.md 지원: 폴더별 맥락 제공 가능 (`src/lib/ai/AGENTS.md` 등)
- 같은 실수가 반복될 때 rule 추가

#### Project Rules (.cursor/rules/*.mdc)

```
# .cursor/rules/react-components.mdc
---
globs: "src/components/**/*.tsx"
---

- 모든 컴포넌트는 함수형으로 작성
- Props는 interface로 정의
- 컴포넌트 파일당 하나의 export default
- TailwindCSS 클래스 사용, inline style 금지
```

```
# .cursor/rules/api-routes.mdc
---
globs: "src/app/api/**/*.ts"
---

- 모든 API 라우트는 try-catch로 감싸기
- 에러 응답은 { error: string, code: number } 형태
- Supabase 클라이언트는 lib/supabase.ts에서 import
```

---

### 2.4 MCP (Model Context Protocol)

외부 도구와 AI를 연결하는 표준 인터페이스. "AI의 USB-C 포트."

**관리 명령어:**

```
/mcp list     → 서버 목록 확인
/mcp enable   → 활성화
/mcp disable  → 비활성화
```

**설정 방식:**
- 프로젝트별: `.cursor/mcp.json`
- 글로벌: Cursor Settings → MCP Tools

**해커톤 필수 MCP 서버:**

| MCP 서버 | 용도 | 왜 필요한가 |
|---|---|---|
| **Browser MCP** | 브라우저 스크린샷, DOM, 콘솔 캡처 | UI 버그를 에이전트가 직접 확인 → 수동 테스트 시간 절약 |
| **Supabase MCP** | DB 스키마/쿼리 에디터 내 실행 | DB 관리 화면 왔다갔다 안 해도 됨 |
| **Vercel MCP** | 배포 자동화 | 원클릭 배포로 데모 준비 |

**선택 MCP 서버:**

| MCP 서버 | 용도 |
|---|---|
| **Figma MCP** | 디자인 → 코드 변환 (디자인 있으면) |
| **GitHub MCP** | 이슈/PR 관리 |

---

### 2.5 Hooks

에이전트 루프의 핵심 시점에 커스텀 스크립트를 자동 실행.

| 시점 | 용도 | 해커톤 추천 |
|---|---|---|
| **파일 저장 후** | prettier, eslint --fix | ✅ 필수 |
| **명령 실행 전** | 위험 명령 차단 | 선택 |
| **커밋 전** | 린트 체크 | 선택 |

---

### 2.6 Cloud Agents

- 에디터에서 클라우드로 에이전트를 보내 실행
- **99.9% 안정성**, 즉시 시작
- 로컬 리소스 부담 없이 긴 작업을 백그라운드 처리

---

### 2.7 Browser Integration

- 에이전트가 실제 브라우저와 통신 (MCP 기반)
- 스크린샷, DOM 노드, 콘솔 로그, 네트워크 요청 캡처
- **UI 버그를 에이전트가 직접 확인하고 수정** → 수동 QA 시간 대폭 절약

---

### 2.8 Sandboxing

- macOS/Linux에서 에이전트 명령을 보안 샌드박스에서 실행
- 워크스페이스만 읽기/쓰기 가능, 인터넷 접근 차단

---

## 3. wigtn 플러그인 경험의 Cursor 전환

우리가 Claude Code 플러그인으로 쌓은 경험을 Cursor에서 어떻게 활용할 수 있는가.

### 우리가 만든 것 (Claude Code 플러그인)

| 구성요소 | 수량 | 예시 |
|---|---|---|
| **Plugins** | 6개 (5개 퍼블리시) | public-commands, frontend, backend, mobile, ai-development |
| **Commands** | 13개 | /prd, /implement, /auto-commit, /backend, /devops 등 |
| **Skills** | 47개 | design-skill, nextjs-app-router, react-hooks, seo 등 |
| **Agents** | 8개 | architecture-decision, code-formatter, design-discovery 등 |

### 워크플로우 파이프라인

```
/prd → digging → /implement → /auto-commit
(기획)  (분석)    (구현)       (리뷰+커밋)
```

### 1:1 매핑 비교

| wigtn (Claude Code) | Cursor 대응 기능 | 전환 방법 |
|---|---|---|
| **Subagents** (architecture-decision 등) | **Parallel Agents** | 아키텍처 결정을 병렬 에이전트로 여러 안 동시 생성 |
| **Commands** (/prd, /implement) | **AGENTS.md + Rules** | 워크플로우를 AGENTS.md에 자연어로 정의 |
| **Skills** (47개 도메인 스킬) | **Project Rules (.mdc)** | 핵심 패턴을 glob 스코핑된 규칙으로 변환 |
| **Hooks** (pre/post 동작) | **Hooks** | 동일 개념, 직접 이식 가능 |
| **Quality Gate** (스코어링) | **Rules + Hooks 조합** | 커밋 전 린트/타입체크 Hooks로 구현 |
| **Plugin Marketplace** | **MCP Servers** | 외부 도구 연동은 MCP로 |
| **Plan Mode** | **Plan Mode** | 동일 개념 (Shift+Tab) |
| **Progressive Disclosure** | 해당 없음 | 우리만의 차별점 |

### 전환 시 핵심 차이

```
Claude Code: 마크다운 파일 기반, 구조화된 플러그인 시스템
Cursor:      AGENTS.md + .cursor/rules/ 기반, 더 자유로운 형태

우리의 강점:
→ 47개 스킬에 담긴 "도메인 지식"을 Rules로 녹여내면
→ 다른 팀 대비 AI 출력 품질에서 압도적 우위
```

---

## 4. 사전 준비 (코드 없이)

> **규칙:** 코드는 당일 현장에서 시작. 아래는 모두 코드가 아닌 **설정/기획** 작업.

### 4.1 아이디어 & 기획 (가장 중요)

```
□ 아이디어 확정 + 한 문장 정의
  예: "AI가 사용자의 일정을 분석해 최적의 할 일 순서를 추천하는 서비스"

□ 핵심 기능 3개 정의 (5시간에 3개가 MAX)
  1. [핵심] ________________
  2. [핵심] ________________
  3. [보너스] ______________

□ 사용자 플로우 정의 (데모 시나리오가 됨)
  Step 1: 사용자가 _____ 한다
  Step 2: AI가 _____ 한다
  Step 3: 결과를 _____ 로 보여준다

□ DB 스키마 초안 설계 (테이블/컬럼 수준)

□ API 엔드포인트 목록 정의
  POST /api/analyze  → AI 분석 요청
  GET  /api/results  → 결과 조회
  ...

□ 와이어프레임 (손그림 수준이라도)
  - 메인 페이지
  - 입력 페이지
  - 결과 페이지

□ 역할 분배 확정 (AI-1, AI-2, FE-1, FE-2)
```

### 4.2 심사 기준 맞춤 기획

```
[실용성] "이거 실제로 쓰겠는데?" 라는 반응을 끌어내려면:
→ 실제 페인 포인트에서 출발
→ "내가/우리 팀이 당장 쓸 수 있는가?"로 검증

[창의성] 문제 정의 자체가 새로워야:
→ 흔한 주제(Todo, 챗봇)에 독특한 관점 추가
→ "이 문제를 이렇게 풀 생각은 못했다" 반응 유도

[완성도] 5시간 안에 데모가 안정적으로 돌아야:
→ 기능 수를 3개로 제한
→ happy path만 완벽하게

[임팩트] 확장 가능성을 보여줘야:
→ 발표에서 "향후 이렇게 발전 가능" 슬라이드 1장
→ 시장 규모나 타겟 사용자 언급
```

### 4.3 AGENTS.md 사전 작성

```
□ 프로젝트 정의 (이름, 한 줄 설명, 아키텍처)
□ 폴더 구조 정의
□ 코딩 컨벤션 정의
□ 각 팀원 담당 영역 명시
□ DB 스키마 명시
□ API 엔드포인트 명시
```

### 4.4 .cursor/rules/ 사전 작성

```
□ react-components.mdc  → 컴포넌트 작성 규칙
□ api-routes.mdc        → API 라우트 규칙
□ ai-integration.mdc    → AI API 호출 패턴
□ styling.mdc           → TailwindCSS 사용 규칙
□ typescript.mdc        → TypeScript 컨벤션
```

### 4.5 환경 설정

```
□ Cursor 최신 버전 (2.2+) 4명 전원 설치
□ Cursor Pro 계정 확인 (Parallel Agents 필요)
□ Git worktree 설정 확인 (좌하단 local → worktree)
□ Node.js 20+ 설치
□ 모델 설정: Claude Sonnet 4 또는 GPT-4o (속도 우선)
```

### 4.6 MCP 서버 사전 설치 + 인증

```
□ Supabase MCP 설치 + 인증 완료
□ Vercel MCP 설치 + 인증 완료
□ Browser MCP 설치
□ /mcp list 로 전부 🟢 확인
```

### 4.7 Team Rules 설정

```
□ Cursor Dashboard에서 Team 생성
□ 4명 초대 완료
□ 공통 규칙 등록
```

### 4.8 개인 준비

```
□ OpenAI / Anthropic API 키 확보 (크레딧 충분한지 확인)
□ Supabase 프로젝트 생성 + API 키 (프로젝트는 미리 만들어도 됨)
□ Vercel 계정 + 토큰
□ .env.example 파일 내용 정리 (키 이름만, 값은 당일)
□ 각자 Cursor User Rules 설정
```

### 4.9 사전 리허설 (강력 추천)

```
□ 팀원 4명 모여서 1시간짜리 미니 해커톤 시뮬레이션
□ 빈 프로젝트에서 시작 → 30분 안에 Next.js + Supabase + AI 붙이기
□ Parallel Agents 실제 사용해보기
□ MCP 서버 연동 테스트
□ Cursor 단축키 & 기능 숙달
□ 병목 구간 파악 → AGENTS.md/Rules에 보완
```

---

## 5. 당일 5시간 실전 전략

### 타임라인

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  시간          │ Phase        │ 내용                    │ 비고
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  09:30 - 10:00 │              │ 체크인                   │
  10:00 - 10:30 │              │ 오프닝                   │ Cursor 팁 메모
  10:30 - 11:00 │ Pre          │ 아이디어 브리핑           │ 팀 최종 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  11:00 - 11:15 │ Phase 0      │ 프로젝트 셋업            │ ⚡ 15분 컷
  11:15 - 13:30 │ Phase 1      │ 핵심 기능 개발 (전반)     │ 병렬 풀가동
  13:30 - 14:00 │ ⚡ 점심       │ 중간 머지 + 점심          │ 머지 필수
  14:00 - 15:00 │ Phase 2      │ 통합 + 핵심 기능 (후반)   │ E2E 연결
  15:00 - 15:30 │ Phase 3      │ 폴리싱 + 데모 최적화      │ 새 기능 금지
  15:30 - 16:00 │ Phase 4      │ 최종 제출 준비            │ 배포 + 데모 확인
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  16:00 - 17:40 │              │ 발표 & 심사               │ 팀당 3~4분
  17:40 - 18:00 │              │ 시상 & 네트워킹            │
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

---

### Phase 0: 프로젝트 셋업 (11:00 ~ 11:15) — 15분 컷

> 사전 준비가 완벽하면 15분 안에 끝나야 한다.

**FE-1 (리드):**
```
1. npx create-next-app@latest → TypeScript + TailwindCSS + App Router
2. AGENTS.md 복사 (사전 준비본)
3. .cursor/rules/ 복사 (사전 준비본)
4. git init && git commit -m "init"
5. GitHub repo 생성 + push
6. Vercel 연결 + 첫 배포
```

**FE-2 (동시):**
```
1. Supabase 스키마 마이그레이션 실행 (SQL은 사전에 작성해둠)
2. .env.local 생성 (키 값 입력)
3. Supabase 클라이언트 설정 파일 작성
```

**AI-1 (동시):**
```
1. AI API 클라이언트 설정 파일 작성
2. .env.local에 API 키 추가
```

**AI-2 (동시):**
```
1. AGENTS.md에 당일 확정 사항 반영
2. 프롬프트 템플릿 파일 생성
3. 전원 git clone 완료 확인
```

**Output: 11:15에 전원 동일한 프로젝트에서 코딩 시작 가능 상태**

---

### Phase 1: 핵심 기능 개발 - 전반 (11:15 ~ 13:30) — 2시간 15분

> 가장 중요한 구간. **Parallel Agents 최대 활용.**
> 각자 독립된 feature 브랜치에서 작업.

| 담당 | 작업 | Parallel Agents 활용 |
|---|---|---|
| **AI-1** | 핵심 AI 기능 구현 (모델 호출, 응답 파싱, 구조화) | 여러 모델/프롬프트 접근법 동시 시도 |
| **AI-2** | 프롬프트 엔지니어링 + 테스트 하네스 | 프롬프트 변형 여러 개 병렬 평가 |
| **FE-1** | 핵심 UI 3개 페이지 (메인, 입력, 결과) | 3개 페이지를 병렬 에이전트로 동시 생성 |
| **FE-2** | API 라우트 + DB CRUD + 인증 (선택) | CRUD 엔드포인트 병렬 생성 |

**FE-1 Parallel Agents 구체적 활용:**

```
Cursor에서 worktree 모드로 3x 병렬 실행:

Agent 1: "src/app/page.tsx에 메인 랜딩 페이지를 만들어줘.
          히어로 섹션 + CTA 버튼 + 간단한 설명"

Agent 2: "src/app/input/page.tsx에 사용자 입력 폼 페이지를 만들어줘.
          [구체적 입력 필드 설명]"

Agent 3: "src/app/result/page.tsx에 AI 분석 결과 표시 페이지를 만들어줘.
          로딩 상태 + 결과 카드 + 공유 버튼"

→ 3개 동시 완성 → Auto-Judging → 채택
→ 순차: 2시간+ → 병렬: 40분
```

**AI-1 작업 상세:**

```
1. src/lib/ai/client.ts → AI API 클라이언트 (OpenAI/Anthropic)
2. src/lib/ai/prompts.ts → 프롬프트 템플릿
3. src/lib/ai/parser.ts → 응답 파싱/구조화
4. src/app/api/analyze/route.ts → AI 분석 API 엔드포인트
5. 스트리밍 응답 구현 (UX 체감 속도 향상)
```

---

### ⚡ 중간 머지 + 점심 (13:30 ~ 14:00) — 30분

> **이 30분이 해커톤 성패를 가른다.** 먹으면서 머지한다.

```
필수 체크리스트:
□ 전원 feature 브랜치 → main 머지
□ npm run build 성공 확인
□ 핵심 플로우 end-to-end 동작 확인:
  입력 → API 호출 → AI 응답 → 결과 표시
□ Vercel 배포 확인 (프리뷰 URL로)
□ 안 되는 것 목록 작성 → 우선순위 재조정
□ AGENTS.md 업데이트 (현재 상태 반영)
```

**머지 후 판단 기준:**

```
E2E가 동작함 → Phase 2에서 고도화 + 추가 기능
E2E가 안 됨  → Phase 2에서 통합에 올인, 추가 기능 포기
```

---

### Phase 2: 통합 + 핵심 기능 후반 (14:00 ~ 15:00) — 1시간

**팀을 2조로 재편성하여 교차 통합:**

| 조 | 구성 | 작업 |
|---|---|---|
| **통합 A** | AI-1 + FE-2 | AI ↔ API ↔ DB 파이프라인 완성 + 에러 핸들링 |
| **통합 B** | AI-2 + FE-1 | UI에 AI 결과 렌더링 + 로딩/에러 상태 + UX 다듬기 |

```
통합 시 Cursor 활용:
- Browser MCP: 실제 브라우저에서 동작 확인 (스크린샷 → AI가 버그 발견)
- Ask Mode: 통합 이슈 빠르게 디버깅
- Agent Mode: 에러 핸들링 코드 자동 생성
```

```
통합 완료 기준:
□ 사용자 입력 → AI 분석 → 결과 표시 (happy path 완벽 동작)
□ 로딩 중 스피너/스켈레톤 표시
□ 에러 시 사용자 친화적 메시지
□ Vercel 배포 URL에서 정상 동작
```

---

### Phase 3: 폴리싱 + 데모 최적화 (15:00 ~ 15:30) — 30분

> **절대 규칙: 새 기능 추가 금지. 있는 것을 완벽하게.**

| 담당 | 작업 |
|---|---|
| **FE-1** | UI 폴리싱: 색상, 간격, 폰트, 반응형, 애니메이션 |
| **FE-2** | 엣지 케이스 방어: 빈 입력, 긴 텍스트, 네트워크 에러 |
| **AI-1** | 응답 속도 최적화: 캐싱, timeout, fallback |
| **AI-2** | **데모 시나리오 최적화**: 데모용 입력 데이터, 예상 질문 준비 |

**AI-2의 역할이 여기서 핵심:**

```
- 발표에서 보여줄 정확한 시나리오 3개 준비
- 각 시나리오 직접 테스트 → AI 응답이 인상적인 케이스 선별
- 데모용 seed 데이터 입력
- 발표 스크립트 초안 작성
```

---

### Phase 4: 최종 제출 준비 (15:30 ~ 16:00) — 30분

| 담당 | 작업 |
|---|---|
| **AI-1 + FE-2** | 최종 Vercel 배포 + 동작 확인 + README 작성 |
| **FE-1** | 스크린샷 촬영 + 발표용 화면 준비 |
| **AI-2** | 발표 스크립트 확정 + 리허설 1회 |

```
제출 전 최종 체크:
□ Vercel 배포 URL 정상 동작
□ 데모 시나리오 3개 전부 정상 동작
□ README에 프로젝트 설명 + 실행 방법
□ 발표자(AI-2) 리허설 완료
□ 타이머 3분 30초 맞춰서 발표 연습
```

---

## 6. 발표 전략 (3~4분)

### 구성 (3분 30초 기준)

```
[0:00 - 0:30] 문제 정의 — 30초
  "여러분 _____ 할 때 _____ 한 경험 있으시죠?"
  → 공감 유도 → 문제 크기 보여주기
  → 창의성 점수를 여기서 획득

[0:30 - 0:50] 솔루션 소개 — 20초
  "저희는 AI를 활용해 _____를 _____하는 서비스를 만들었습니다."
  → 한 문장으로 설명
  → 실용성 + 임팩트 어필

[0:50 - 3:00] 라이브 데모 — 2분 10초
  시나리오 1: 핵심 기능 시연 (가장 인상적인 것)
  시나리오 2: AI 분석 결과 보여주기
  시나리오 3: (시간 되면) 추가 기능
  → 완성도 점수를 여기서 획득
  → 실제 배포된 URL에서 시연 (로컬 X)

[3:00 - 3:30] 기술 스택 + 향후 계획 — 30초
  "Next.js + Supabase + [AI 모델]로 5시간 만에 구축"
  "향후 _____ 기능 추가하면 _____ 시장에서 활용 가능"
  → 임팩트 점수를 여기서 획득
```

### 발표 팁

```
- 절대 코드를 보여주지 말 것 → 동작하는 화면만 보여줄 것
- 데모는 반드시 배포된 URL에서 (로컬 서버 불안정)
- 데모가 실패할 경우를 대비해 스크린샷/영상 백업
- "Cursor의 Parallel Agents로 5시간 만에 가능했다" 한마디 추가
  → Cursor 해커톤이므로 도구 활용 어필
- 심사위원 눈을 보면서 발표 (화면만 보지 말 것)
```

---

## 7. 핵심 팁

### 전략적 팁 (순위별)

| 순위 | 팁 | 이유 |
|---|---|---|
| 1 | **기능은 3개로 제한** | 5시간에 5개 하려다 0개 완성하는 팀이 대부분. 3개를 완벽하게. |
| 2 | **사전 준비 = 승패** | AGENTS.md, Rules, MCP, 스키마 설계를 미리 하면 당일 30분 절약. 30분 = 전체의 10%. |
| 3 | **13:30 중간 머지 반드시** | 통합 이슈를 후반에 발견하면 치명적. 절반 시점에 E2E 확인 필수. |
| 4 | **Parallel Agents = 시간 3배** | 페이지 3개를 순차로 만들면 2시간, 병렬이면 40분. |
| 5 | **데모 > 완성도** | 80% 완성된 3개 기능이 100% 완성된 1개보다 낫다. |
| 6 | **15:00 이후 새 기능 금지** | 있는 것을 깨뜨리지 말 것. 데모 경로만 완벽하게. |
| 7 | **AI-2 = 발표자 겸 QA** | 후반부에 코딩보다 데모 시나리오 최적화에 집중. 발표가 점수를 결정. |
| 8 | **Plan Mode 먼저** | 11:00에 바로 코딩하지 말 것. 10분만 Plan Mode 쓰면 나머지 4시간 50분의 품질이 달라짐. |
| 9 | **Browser MCP 적극 활용** | 에이전트가 직접 브라우저에서 UI 확인 → 수동 테스트 시간 절약. |
| 10 | **배포는 초반에** | 11:15에 첫 Vercel 배포. 이후 push할 때마다 자동 배포. 마지막에 배포하면 문제 발생 시 대응 불가. |

### Cursor 실전 팁

```
1. Shift+Tab (Plan Mode) → 작업 시작 전 항상 계획부터
2. worktree 모드 → Parallel Agents 활성화 필수
3. AGENTS.md → 새 채팅 열 때마다 컨텍스트 자동 로딩
4. .cursor/rules/*.mdc → glob으로 파일별 규칙 자동 적용
5. /mcp list → MCP 서버 상태 수시 확인
6. Browser MCP → UI 변경 후 에이전트가 직접 확인
7. Ask Mode → 디버깅 시 코드 수정 없이 분석만 요청
8. Cloud Agents → 무거운 작업은 클라우드로 보내서 로컬 부담 감소
```

---

## 8. 참고 자료

### Cursor 공식

- [Cursor Features](https://cursor.com/features)
- [Cursor 2.0 Changelog](https://cursor.com/changelog/2-0)
- [Cursor 2.2: Multi-Agent Judging](https://cursor.com/changelog/2-2)
- [Cursor Rules Docs](https://cursor.com/docs/context/rules)
- [Agent Modes Docs](https://cursor.com/docs/agent/modes)
- [Best Practices for Coding with Agents](https://cursor.com/blog/agent-best-practices)

### MCP 관련

- [MCP Servers for Cursor](https://cursor.directory/mcp)
- [Cursor Directory - Rules & MCP Servers](https://cursor.directory/)
- [Build Custom MCP Server](https://apidog.com/blog/build-a-custom-mcp-server/)
- [5 Essential MCP Servers](https://plainenglish.io/blog/5-essential-mcp-servers-that-give-claude-cursor-real-superpowers-2025)

### 전략 & Best Practices

- [Cursor AI Review 2026](https://prismic.io/blog/cursor-ai)
- [Cursor Changelog: What's Coming Next](https://blog.promptlayer.com/cursor-changelog-whats-coming-next-in-2026/)
- [AGENTS.md Best Practices](https://kau.sh/blog/agents-md/)
- [Cursor Rules Best Practices](https://medium.com/elementor-engineers/cursor-rules-best-practices-for-developers-16a438a4935c)
- [Agent Modes Deep Dive](https://developertoolkit.ai/en/cursor-ide/advanced-techniques/agent-modes-deep-dive/)
