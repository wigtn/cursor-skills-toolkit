# Cursor Skills Toolkit

> Cursor AI 해커톤을 위한 통합 개발 킷 — 규칙, 타입, 명령, 가이드 올인원

**Cursor Seoul Hackathon 2026** | 4명 팀 | Cursor AI 바이브코딩

---

## 이 레포는 무엇인가?

해커톤에서 4명이 동시에 Cursor AI로 개발할 때 필요한 **모든 설정 파일**을 모아둔 툴킷입니다.

포함 내용:
- **Cursor AI 규칙** (.cursorrules + .mdc 8개) — 역할별 자동 로드
- **태스크 커맨드** (6개) — 역할별 전체 작업 목록
- **공유 타입/상수/메시지 규격** (shared/) — Single Source of Truth
- **에러 해결 지식베이스** — 자주 발생하는 문제와 해결법
- **해커톤 가이드** — 타임라인, 아키텍처, 긴급 대응

---

## 현재 프로젝트: Stone Buddy (힐링 게임)

**로봇 집사에게 채팅으로 명령하여 내 돌을 돌보고, 코인으로 테라리움을 꾸미며, 돌에게 말을 걸면 우편함에 편지로 답장이 오는 힐링 게임**

### 핵심 게임 루프

```
1. 채팅 명령 → 또봇 AI 해석 → 10개 워크플로우 수행 → 돌돌이 감정 변화
2. 코인 적립 (1분/1코인) → 상점에서 아이템 구매 → "~해줘" 요청 → 적용
3. 음성으로 돌에게 말 걸기 → 대기(10초) → 우편함에 편지 도착
```

### 아키텍처

```
┌──────────────────────────────────────────┐
│            Expo App (FE-1)               │
│  ┌────────────────────────────────────┐  │
│  │         WebView (전체 화면)         │  │
│  │  ┌──────────────────────────────┐  │  │
│  │  │   Vite + React + R3F        │  │  │
│  │  │   3D 씬 (FE-2)              │  │  │
│  │  │   - Robot(또봇) + Stone(돌돌이)│  │  │
│  │  │   - Background + Mailbox    │  │  │
│  │  │   App.tsx 로직 (AI-1)       │  │  │
│  │  │   - 또봇 AI 명령 해석        │  │  │
│  │  │   - 돌돌이 편지 생성 (LLM)   │  │  │
│  │  └──────────────────────────────┘  │  │
│  │  ← postMessage로 통신              │  │
│  └────────────────────────────────────┘  │
│  ┌────────────────────────────────────┐  │
│  │  [채팅 입력] [전송] [🎤 녹음]     │  │
│  └────────────────────────────────────┘  │
└──────────────────────────────────────────┘

shared/ (AI-2): 타입, 메시지 규격, 상수, 아이템 카탈로그
```

### 기술 스택

| 영역 | 기술 |
|------|------|
| 앱 | Expo SDK 54, react-native-webview, expo-audio |
| 웹 | Vite 7, React 19, @react-three/fiber 9, drei 10 |
| AI | OpenAI GPT-4o-mini (명령 해석 + 편지), Whisper (STT) |
| 상태 | Zustand + AsyncStorage |
| 타입 | TypeScript strict mode |

---

## 폴더 구조

```
cursor-skills-toolkit/
│
├── .cursorrules                     # 마스터 규칙 (항상 자동 로드)
│
├── .cursor/
│   ├── rules/                       # Cursor AI 규칙 (.mdc)
│   │   ├── fe1-expo-native.mdc      #   FE-1: Expo 네이티브 (globs: app/**)
│   │   ├── fe2-3d-web.mdc           #   FE-2: 3D 웹 씬 (globs: web/src/components/**)
│   │   ├── ai1-chat-state.mdc       #   AI-1: 채팅/상태 (globs: web/src/App.tsx 등)
│   │   ├── ai2-types-docs.mdc       #   AI-2: 타입/문서 (globs: shared/**, docs/**)
│   │   ├── commit-review.mdc        #   커밋 품질 검사
│   │   ├── team-workflow.mdc         #   팀 워크플로우 (alwaysApply)
│   │   └── troubleshooting.mdc       #   트러블슈팅 (alwaysApply)
│   │
│   ├── commands/                    # 역할별 태스크 커맨드
│   │   ├── phase1-setup.md          #   전원: 환경 설정
│   │   ├── fe1-all-tasks.md         #   FE-1: Expo 네이티브 전체 태스크
│   │   ├── fe2-all-tasks.md         #   FE-2: 3D 웹 씬 전체 태스크
│   │   ├── ai1-all-tasks.md         #   AI-1: AI 채팅/편지 전체 태스크
│   │   ├── ai2-all-tasks.md         #   AI-2: 타입/상점/코인 전체 태스크
│   │   └── auto-commit.md           #   자동 커밋 프로세스
│   │
│   ├── knowledge/                   # 에러 해결 지식베이스
│   │   └── error-solutions.md       #   해결된 에러 기록
│   │
│   └── mcp.json                     # MCP 서버 설정
│
├── shared/                          # 공유 타입·상수·메시지 규격
│   ├── types.ts                     #   타입 (10 워크플로우, 아이템, 편지, GameState)
│   ├── constants.ts                 #   상수 (코인, 편지, 아이템 카탈로그 13종)
│   └── message-protocol.ts          #   WebView↔Native 메시지 프로토콜 (LETTER_READY 포함)
│
├── docs/
│   ├── prd/
│   │   └── PRD_stone-garden.md      #   PRD v6.0 (Healing Game Edition)
│   ├── todo_plan/
│   │   └── PLAN_stone-garden.md     #   태스크 플랜 (PRD v6.0 기반)
│   └── archive/                     #   이전 기획 문서
│
├── scripts/
│   └── init-project.sh              #   프로젝트 초기화 스크립트
│
├── HACKATHON-GUIDE.md               # 해커톤 퀵 레퍼런스 (10 워크플로우, 코인, 편지)
├── .env.example                     # 환경변수 템플릿
└── .gitignore
```

---

## 역할 배정

| 역할 | 담당 폴더 | 핵심 작업 |
|------|-----------|-----------|
| **FE-1** | `app/` | Expo 앱, WebView, 채팅 입력, 녹음, STT |
| **FE-2** | `web/src/components/`, `web/public/`, CSS | 3D 씬, 돌돌이, 또봇, 우편함, 아이템 |
| **AI-1** | `web/src/App.tsx`, `hooks/`, `stores/`, `lib/` | 또봇 AI 연동, 편지 LLM, 상태 관리 |
| **AI-2** | `shared/`, `docs/`, `.cursor/` | 타입, 상점 UI, 코인 시스템, 문서 |

---

## 시작하기

### 1. 이 레포 클론

```bash
git clone https://github.com/wigtn/cursor-skills-toolkit.git
cd cursor-skills-toolkit
```

### 2. 프로젝트 초기화

```bash
# 자동 초기화 (app/, web/ 폴더 생성)
bash scripts/init-project.sh

# 또는 수동으로
cd app && npx create-expo-app@latest . --template blank-typescript && npm install react-native-webview expo-audio expo-av && cd ..
cd web && npm create vite@latest . -- --template react-ts && npm install three @react-three/fiber @react-three/drei zustand && cd ..
```

### 3. 환경변수 설정

```bash
cp .env.example .env
# .env에 EXPO_PUBLIC_OPENAI_API_KEY 입력 (없어도 더미로 동작)
```

### 4. GLB 모델 다운로드

```bash
mkdir -p web/public/models
curl -L -o web/public/models/RobotExpressive.glb \
  "https://github.com/mrdoob/three.js/raw/dev/examples/models/gltf/RobotExpressive/RobotExpressive.glb"
```

### 5. 실행

```bash
# 터미널 1: 웹 개발 서버
cd web && npm run dev          # → http://localhost:5173

# 터미널 2: Expo 앱
cd app && npx expo start       # → 시뮬레이터/기기
```

---

## Cursor AI 사용법

### 규칙 자동 로딩

| 문서 유형 | 자동 로드 | 수동 호출 |
|----------|----------|----------|
| `.cursorrules` | 항상 | - |
| `.mdc` (alwaysApply: true) | 항상 | - |
| `.mdc` (globs 매칭) | 해당 파일 작업 시 | `@파일명` |
| `commands/*.md` | X | `@파일명` 또는 파일 열기 |

### 프롬프트 템플릿

```
나는 [FE-1/FE-2/AI-1/AI-2]야.
.cursorrules와 .cursor/rules/[내 규칙 파일].mdc를 따라줘.
[내 담당 폴더]만 수정해. 다른 폴더는 절대 건드리지 마.

[요청 내용]
```

### 태스크 실행

```
@fe1-all-tasks.md 이 태스크 목록 진행해줘
```

---

## 타임라인 (6시간)

| 시간 | Phase | 작업 | 담당 |
|------|-------|------|------|
| 0:00~0:20 | Setup | 환경설정, GLB 검증, API 키 | 전원 |
| 0:20~2:00 | Phase 2 | 3D씬 + Expo앱 + 채팅 (병렬) | FE-1, FE-2 |
| 2:00~3:30 | Phase 3 | AI 명령해석 + 코인/상점 + 편지 | AI-1, AI-2 |
| 3:30~4:30 | Phase 4 | 감정 시스템 + 편지 + 통합 | 전원 |
| 4:30~5:00 | Phase 5 | 폴리싱 + 버그 수정 | 전원 |
| 5:30~6:00 | 시연 | 빌드 + 발표 준비 | 전원 |

---

## 주요 문서

| 문서 | 설명 |
|------|------|
| [HACKATHON-GUIDE.md](./HACKATHON-GUIDE.md) | 해커톤 퀵 레퍼런스 (10 워크플로우, 코인, 편지, 긴급 대응) |
| [.cursorrules](./.cursorrules) | 마스터 규칙 (게임 루프, 메시지 규격, 코딩 컨벤션) |
| [PRD](./docs/prd/PRD_stone-garden.md) | 기획 문서 v6.0 (Healing Game Edition) |
| [PLAN](./docs/todo_plan/PLAN_stone-garden.md) | 태스크 플랜 (PRD v6.0 기반) |
| [error-solutions.md](./.cursor/knowledge/error-solutions.md) | 에러 해결 지식베이스 |

---

## Git 브랜치 전략

```
main                          ← 문서·규칙 (현재)
├── feature/phase1-setup      ← 전원: 환경 설정
├── feature/phase2-3d         ← FE-2: R3F 3D 씬
├── feature/phase2-native     ← FE-1: Expo WebView + 채팅
├── feature/phase3-chat       ← AI-1: AI 연동 + 편지
├── feature/phase3-content    ← AI-2: 코인 + 상점
├── feature/phase4-routine    ← 전원: 감정 + 통합
└── feature/phase5-polish     ← 전원: 폴리싱
```

머지 순서: `shared/types` → Phase 2 (native → 3D) → Phase 3 (content → chat) → Phase 4 → Phase 5

---

## 기여자

| 역할 | 담당 |
|------|------|
| FE-1 | Expo 네이티브 + 채팅 입력 + STT |
| FE-2 | 3D 웹 씬 + 돌돌이 + 우편함 |
| AI-1 | 또봇 AI 연동 + 편지 LLM |
| AI-2 | 타입/상점/코인/문서 |
