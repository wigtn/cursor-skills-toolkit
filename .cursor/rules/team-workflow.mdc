---
description: 4명 팀 협업 워크플로우 — Git, 머지 순서, 충돌 방지
globs: "**/*"
alwaysApply: true
---

# 팀 워크플로우 (4명 동시 개발)

## 역할 배정

| 역할 | 담당자 | 전용 폴더 | 수정 금지 |
|------|--------|-----------|-----------|
| **FE-1** | Expo/네이티브 | `app/` | `web/`, `shared/types`, `docs/` |
| **FE-2** | 3D/웹 씬 | `web/src/components/`, `web/public/`, CSS | `app/`, `web/src/App.tsx`, `shared/types` |
| **AI-1** | AI/채팅/상태 | `web/src/App.tsx`, `web/src/main.tsx`, hooks, stores, lib | `app/`, `web/src/components/`, `shared/types` |
| **AI-2** | 타입/문서/통합 | `shared/`, `docs/`, `.cursor/`, README | `app/`, `web/src/` |

## 공유 파일 (수정 시 팀 합의 필수)
- `shared/types.ts` — AI-2가 관리, 변경 시 전원 합의
- `shared/message-protocol.ts` — 메시지 규격, 절대 함부로 변경 금지
- `shared/constants.ts` — 상수 추가는 자유, 기존 값 변경은 합의

## 📌 단일 소스 원칙 (Single Source of Truth)
**아래 항목들은 반드시 shared/에서 import해서 사용!**
- `ROBOT_ANIMATIONS`, `RobotAnimation` → shared/types.ts
- `ANIMATION_INFO` → shared/types.ts
- `SPIRIT_FALLBACK_RESPONSES` → shared/constants.ts
- `OPENAI_CONFIG` → shared/constants.ts
- `BridgeMessage`, 타입 가드 함수들 → shared/message-protocol.ts

**❌ 각 컴포넌트에서 재정의 금지!**

## Git 브랜치 전략

### 브랜치 구조
```
main                          ← 문서·규칙만 (앱 코드 없음)
├── feature/phase1-setup      ← 전원: 환경 설정
├── feature/phase2-3d         ← FE-2: R3F 씬
├── feature/phase2-native     ← FE-1: Expo WebView
├── feature/phase3-chat       ← AI-1: OpenAI 채팅
├── feature/phase3-content    ← AI-2: 콘텐츠·프롬프트
├── feature/phase4-routine    ← 전원: 감정 루틴
└── feature/phase5-polish     ← 전원: 폴리싱
```

### 머지 순서 (반드시 준수)
```
1. shared/types (AI-2)        ← 타입 먼저 확정
2. Phase 2: native → 3D       ← FE-1 먼저 머지, FE-2 rebase 후 머지
   ⚠️ 2:00 통합 체크포인트: WebView에 3D 보이는지 확인!
3. Phase 3: content → chat    ← AI-2 먼저, AI-1 나중 (의존성)
4. Phase 4: routine           ← 전원 합류
5. Phase 5: polish            ← 전원 합류
```

### shared 변경 시 영향 체크리스트
shared/types.ts 또는 shared/message-protocol.ts 변경 시:
- [ ] FE-1 영향? → app/ 코드 수정 필요?
- [ ] FE-2 영향? → components/ 코드 수정 필요?
- [ ] AI-1 영향? → App.tsx, hooks 코드 수정 필요?
- [ ] 각 역할이 순차적으로 수정 후 머지

### 머지 전 체크리스트
- [ ] 내 영역 파일만 변경했는지 확인
- [ ] TypeScript 에러 없음 (`npx tsc --noEmit`)
- [ ] 메시지 규격 변경 없음 (또는 팀 합의 완료)
- [ ] 슬랙에 "🔀 [브랜치명] 머지 준비" 알림
- [ ] 1명 이상 리뷰 (shared/ 변경 시 전원 리뷰)

## 충돌 방지 규칙

### 구조적 분리
```
FE-1 작업 ──┐
             ├──→ 서로 다른 폴더이므로 충돌 불가
FE-2 작업 ──┘

AI-1 작업 ──┐
             ├──→ 서로 다른 폴더이므로 충돌 불가
AI-2 작업 ──┘
```

### 충돌 가능 지점과 예방
| 충돌 지점 | 원인 | 예방법 |
|-----------|------|--------|
| `shared/types.ts` | 여러 명이 타입 추가 | AI-2만 수정, 다른 사람은 요청 |
| `package.json` | 의존성 추가 | 슬랙에 "📦 [패키지명] 추가할게" 알림 |
| `web/src/App.tsx` | AI-1 외 수정 시도 | AI-1만 수정, FE-2는 components만 |

### 긴급 상황 프로토콜
```
1. "🚨 충돌 발생" 슬랙 알림
2. 해당 파일 소유자가 해결
3. git stash → git pull --rebase → git stash pop
4. 해결 후 "✅ 충돌 해결" 알림
```

## Phase별 타임라인 (8시간 해커톤)

| 시간 | Phase | 작업 | 담당 |
|------|-------|------|------|
| 0:00~0:30 | Phase 1 | 환경 설정, 타입 확정 | 전원 (AI-2 리드) |
| 0:30~3:00 | Phase 2 | 3D 씬 + Expo 앱 병렬 개발 | FE-1 + FE-2 |
| 3:00~5:00 | Phase 3 | AI 채팅 + 콘텐츠 | AI-1 + AI-2 |
| 5:00~6:30 | Phase 4 | 감정 루틴, 연동 | 전원 |
| 6:30~7:30 | Phase 5 | 폴리싱, 버그 수정 | 전원 |
| 7:30~8:00 | 시연 | 빌드, 테스트, 발표 준비 | 전원 |

### 15:00 이후 규칙 (해커톤 후반)
- **새 기능 추가 금지**
- 버그 수정, UI 폴리싱, 발표 준비만

## AI에게 프롬프트할 때 템플릿
```
나는 [FE-1/FE-2/AI-1/AI-2]야.
.cursorrules와 .cursor/rules/[내 규칙 파일].mdc를 따라줘.
[내 담당 폴더]만 수정해. 다른 폴더는 절대 건드리지 마.

[요청 내용]
```
